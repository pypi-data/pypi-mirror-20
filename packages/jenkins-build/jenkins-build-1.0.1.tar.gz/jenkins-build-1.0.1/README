# jenkins-build
A python pip package to trigger/query/cancel/stop/log a jenkins build

# How to build the pip package ?
python setup.py sdist

# How to test local package
pip install ./dist/jenkins-build-x.y.z.tar.gz --upgrade
jenkins-build --help

# How to build and upload to PyPI
python setup.py register 
python setup.py sdist upload

# How to install from PyPI
pip install jenkins-build
