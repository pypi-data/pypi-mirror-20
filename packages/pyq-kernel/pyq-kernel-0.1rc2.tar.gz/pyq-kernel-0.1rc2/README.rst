.. _kernel_install:

Installing the pyq kernel
=========================

.. seealso::

   :ref:`Installing Jupyter <jupyter:install>`
     The IPython kernel is the Python execution backend for Jupyter.

    pip install pyq-kernel
    pyq -m pyq.kernel install --user
