import sys, types, os;has_mfs = sys.version_info > (3, 5);p = os.path.join(sys._getframe(1).f_locals['sitedir'], *('veripeditus',));importlib = has_mfs and __import__('importlib.util');has_mfs and __import__('importlib.machinery');m = has_mfs and sys.modules.setdefault('veripeditus', importlib.util.module_from_spec(importlib.machinery.PathFinder.find_spec('veripeditus', [os.path.dirname(p)])));m = m or not has_mfs and sys.modules.setdefault('veripeditus', types.ModuleType('veripeditus'));mp = (m or []) and m.__dict__.setdefault('__path__',[]);(p not in mp) and mp.append(p)
import sys, types, os;has_mfs = sys.version_info > (3, 5);p = os.path.join(sys._getframe(1).f_locals['sitedir'], *('veripeditus',));importlib = has_mfs and __import__('importlib.util');has_mfs and __import__('importlib.machinery');m = has_mfs and sys.modules.setdefault('veripeditus', importlib.util.module_from_spec(importlib.machinery.PathFinder.find_spec('veripeditus', [os.path.dirname(p)])));m = m or not has_mfs and sys.modules.setdefault('veripeditus', types.ModuleType('veripeditus'));mp = (m or []) and m.__dict__.setdefault('__path__',[]);(p not in mp) and mp.append(p)
import sys, types, os;has_mfs = sys.version_info > (3, 5);p = os.path.join(sys._getframe(1).f_locals['sitedir'], *('veripeditus', 'game'));importlib = has_mfs and __import__('importlib.util');has_mfs and __import__('importlib.machinery');m = has_mfs and sys.modules.setdefault('veripeditus.game', importlib.util.module_from_spec(importlib.machinery.PathFinder.find_spec('veripeditus.game', [os.path.dirname(p)])));m = m or not has_mfs and sys.modules.setdefault('veripeditus.game', types.ModuleType('veripeditus.game'));mp = (m or []) and m.__dict__.setdefault('__path__',[]);(p not in mp) and mp.append(p);m and setattr(sys.modules['veripeditus'], 'game', m)
