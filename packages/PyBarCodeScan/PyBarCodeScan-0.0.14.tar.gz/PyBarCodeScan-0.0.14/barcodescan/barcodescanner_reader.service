[Unit]
Description=Reads barcodes from hardware and send them to barcodescanner_server

[Service]
Type=simple
ExecStart=/venv/bin/barcodescanner_reader >> /var/log/barcodescanner_reader.log 2>&1

[Install]
WantedBy=multi-user.target